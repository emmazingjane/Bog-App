<%= form_for :creature, url: "/creatures", method: "post" do |f| %>
    <%= f.text_field :name %> 
    <%= f.text_area :description %> 
    <%= button_to "Save Creature", {action: "new"}, form_class: "new-thing" %>
<% end %>